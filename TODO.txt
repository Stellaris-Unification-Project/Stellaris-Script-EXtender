Page based event boilerplate
- Simple select values event
- Advanced execute code on choice

List Functions:
- Export list to event targets
- Export script list to list of pointers(no trigger)
- Sort by variable
- Sort by trigger value

In parameters requiring identifiers for searching allow for reference equality checks

Strings:
- String values are substrings with indexes going lowest index as leftmost
- Add at end of index
- Add at index
- Remove at end of index
- Remove at index
- String pattern completion

Events/Constructs:
- Usable keyboard
- Easy paginated event

Move global database entries to an event target's data when creating a new database

Error Handling:
- Retrofit all error messages to their own error strings/messages
- Error handling using string backend

Abstract removals to involve purging by on actions